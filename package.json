{
  "name": "elm-minimal",
  "version": "3.0.1",
  "description": "Minimal template for an Elm project",
  "keywords": ["elm"],
  "license": "MIT",
  "main": "src/Main.elm",
  "scripts": {
    "dev": "elm-live src/Main.elm --dir=www -- --output=www/main.js --debug",
    "build": "elm make src/Main.elm --output=www/main.js --optimize"
  },
  "dependencies": {
    "elm": "0.19.1-3"
  },
  "devDependencies": {
    "elm-live": "4.0.1"
  }
}
